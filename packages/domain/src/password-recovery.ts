export type PasswordRecoveryStatus = 'active' | 'consumed' | 'expired' | 'revoked';
export interface PasswordRecoveryRecord { readonly id:string; readonly authenticationIdentityId:string; readonly secretHash:string; readonly status:PasswordRecoveryStatus; readonly requestedAt:Date; readonly expiresAt:Date; readonly consumedAt:Date|null; readonly revokedAt:Date|null; }
export interface PasswordRecoveryRepository { create(record:PasswordRecoveryRecord):Promise<void>; findById(id:string):Promise<PasswordRecoveryRecord|undefined>; consume(id:string,consumedAt:Date):Promise<void>; revokeActiveForAuthenticationIdentity(authenticationIdentityId:string,revokedAt:Date):Promise<void>; }
export function isPasswordRecoveryUsable(record:PasswordRecoveryRecord,now:Date):boolean { return record.status==='active'&&record.consumedAt===null&&record.revokedAt===null&&record.expiresAt.getTime()>now.getTime(); }
export function assertPasswordRecoveryBoundary(record:PasswordRecoveryRecord):void { if(!record.id.trim()||!record.authenticationIdentityId.trim()) throw new Error('password recovery identifiers are required'); if(!record.secretHash.trim()) throw new Error('password recovery secret hash is required'); if(record.expiresAt.getTime()<=record.requestedAt.getTime()) throw new Error('password recovery expiry must be after request'); }