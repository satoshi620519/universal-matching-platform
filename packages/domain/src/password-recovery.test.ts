import { describe, expect, it } from 'vitest';
import { assertPasswordRecoveryBoundary, isPasswordRecoveryUsable, type PasswordRecoveryRecord } from './password-recovery.js';
const requestedAt=new Date('2026-09-04T00:00:00.000Z');
const active:PasswordRecoveryRecord={id:'recovery-1',authenticationIdentityId:'identity-1',secretHash:'opaque-secret-hash',status:'active',requestedAt,expiresAt:new Date('2026-09-04T01:00:00.000Z'),consumedAt:null,revokedAt:null};
describe('password recovery boundary',()=>{it('allows active recovery',()=>{expect(()=>assertPasswordRecoveryBoundary(active)).not.toThrow();expect(isPasswordRecoveryUsable(active,new Date('2026-09-04T00:30:00.000Z'))).toBe(true);});it('rejects terminal or expired recovery',()=>{expect(isPasswordRecoveryUsable({...active,status:'consumed',consumedAt:requestedAt},new Date('2026-09-04T00:30:00.000Z'))).toBe(false);expect(isPasswordRecoveryUsable({...active,status:'revoked',revokedAt:requestedAt},new Date('2026-09-04T00:30:00.000Z'))).toBe(false);expect(isPasswordRecoveryUsable(active,new Date('2026-09-04T02:00:00.000Z'))).toBe(false);});it('rejects invalid boundary',()=>{expect(()=>assertPasswordRecoveryBoundary({...active,secretHash:' '})).toThrow('secret');expect(()=>assertPasswordRecoveryBoundary({...active,expiresAt:requestedAt})).toThrow('expiry');});});