# Infrastructure

Milestone 0 local infrastructure is defined in the repository root Docker Compose configuration.

Current services:
- PostgreSQL
- Redis

Production provider selection remains intentionally deferred.
