# Local development environment
# Copy to .env only when you need to override these defaults.

# API HTTP server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Local PostgreSQL from docker-compose.yml
DATABASE_URL=postgresql://universal:universal@localhost:5432/universal_matching
